#include<iostream>

using namespace std;

int main() {
    int m, n, x;
    cin >> m >> n >> x;
    vector<vector<int>> pos(x, vector<int>(2));
    for (int i = 0; i < n; i++) {
        cin >> pos[0][0] >> pos[0][1];
    }


}
